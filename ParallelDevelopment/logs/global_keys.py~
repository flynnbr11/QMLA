import redis
import socket

hostname = socket.gethostname()


global = redis.Redis(host=hostname, port=6379)

global_keys = global.keys()

for k in global_keys:
	print("Node", k, ":", global_keys.get(k))
